# Python Import Modules

This repository contains simple Python examples for importing functions from other files.

- `add_0.py`: defines a simple addition function `add(a, b)`.
- `0-add.py`: imports `add` from `add_0.py` and prints `1 + 2 = 3`.
